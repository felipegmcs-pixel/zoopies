{
  "name": "zoopies",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:create": "wrangler d1 create zoopies-orders",
    "db:init": "wrangler d1 execute zoopies-orders --file=schema.sql",
    "db:init:remote": "wrangler d1 execute zoopies-orders --remote --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.65.0"
  }
}
